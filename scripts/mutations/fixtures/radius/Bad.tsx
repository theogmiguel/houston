// MUTATION FIXTURE: a hand-typed corner radius, which check-radius-tokens.sh
// must refuse. Never imported; scanned only via SCAN_ROOT.
export const Bad = () => <div className="rounded-[7px] bg-black" />
